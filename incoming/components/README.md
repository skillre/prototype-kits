# incoming · components

外部�产落在这里。流程与目录约定见 [`../README.md`](../README.md) 与 [`../.template/`](../.template/)。

## 当前内容

（空 —— 没有待审资产。有资产时，每个条目一个目录：）

```
components/<资产名>/
├── SOURCE.md
├── LICENSE
├── AUDIT.md
└── raw/
```

## 提示

- 放进这里**不代表可以用**。状态默认是 `incoming`。
- 没有 `LICENSE`（或写成 unknown）的条目不予审计。
- `raw/` 里的文件**不得**被任何产品路径引用 —— 请走 adapter。
